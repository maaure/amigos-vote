# Exemplo de configuração para ambiente local
# Copie este arquivo para .env.local e preencha os valores conforme seu ambiente


# Connection string para o Supabase (pegue no dashboard do seu projeto Supabase)
# Vá em Project Settings -> Database -> Connection string -> URI
# Exemplo:
DATABASE_URL_SUPABASE="postgresql://postgres:[YOUR-PASSWORD]@db.<project-ref>.supabase.co:5432/postgres"

# Connection string para um PostgreSQL local
# Exemplo:
DATABASE_URL_LOCAL="postgresql://user:password@localhost:5432/meu-database"

# Variável para controlar qual DB usar (supabase ou local)
# Use "supabase" para conectar ao Supabase ou "local" para conectar ao PostgreSQL local
DB_CLIENT="local"

# Outras variáveis de autenticação (exemplo com GitHub OAuth)
GITHUB_ID=<seu-github-id>
GITHUB_SECRET=<seu-github-secret>
